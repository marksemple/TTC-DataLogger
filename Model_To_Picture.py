import os
from PIL import Image, ImageDraw


def Model_to_Picture(model):
    """ Turn a TTC-waittimes Model into an Image

        Mode:
        Waittimes[line[direction]] = [time1, time2, time3]
    """
    # line:
    # Dir1: t, t, t
    # Dir2: t, t, t

    return

